import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'TP_diginamic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
